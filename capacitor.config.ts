import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.jusmonitordyad',
  appName: 'jusmonitor-dyad',
  webDir: 'dist'
};

export default config;
